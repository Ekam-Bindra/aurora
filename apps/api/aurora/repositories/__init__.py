"""Repositories.

Phase 1 ships an in-memory store so the API is fully runnable and testable without external
datastores. Phase 2 (docs/roadmap/implementation-roadmap.md) replaces this with SQLAlchemy
repositories over PostgreSQL behind the same interface.
"""
