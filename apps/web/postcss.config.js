module.exports = {
  plugins: {
    // Tailwind 4: single PostCSS plugin; vendor prefixing is built in
    // (Lightning CSS), so autoprefixer is no longer needed.
    "@tailwindcss/postcss": {},
  },
};
